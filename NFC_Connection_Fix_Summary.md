# Abbott Libre NFC 连接丢失问题解决方案

## 问题分析

您遇到的 "Tag connection lost" 错误主要是由于以下原因：

1. **读取数据量过大**：原代码尝试读取 244 块 × 8 字节 = 1952 字节的完整 FRAM 数据
2. **缺少延迟机制**：连续读取导致 NFC 连接超时
3. **重试机制不完善**：没有足够的错误恢复策略

## 解决方案

### 1. 实现最小化读取模式（默认模式）

#### 特点：
- **只读取实时血糖值**：仅读取 6-8 个数据块（48-64 字节）
- **避免连接丢失**：大大减少数据传输量，降低超时风险
- **快速响应**：读取速度更快，用户体验更好

#### 技术实现：
```swift
// Libre 1: 读取前 6 个块（48 字节）
let essentialBlocks = 6

// Libre 2+: 读取前 8 个块（64 字节）
let essentialBlocks = 8
```

### 2. 改进的重试机制

- **3 次重试**：失败后自动重试，增加成功率
- **延迟重试**：500ms 延迟避免频繁访问
- **部分数据处理**：即使连接丢失也尝试解析已获取的数据

### 3. 用户界面改进

#### 添加读取模式切换开关：
- **实时模式（推荐）**：🎯 只读取当前血糖值，避免连接丢失
- **完整模式**：📚 读取完整历史数据，可能导致连接丢失

#### 使用方法：
1. 在血糖测量界面可以看到"读取模式"切换开关
2. 默认使用"实时模式"来避免连接问题
3. 如需历史数据，可切换到"完整模式"（需承担连接丢失风险）

## 代码更改

### 主要文件修改：

1. **LibreNFCManager.swift**
   - 添加了 `isMinimalReadMode` 属性
   - 实现了 `readLibre1MinimalData()` 和 `readStandardMinimalData()` 方法
   - 改进了重试和延迟机制

2. **NFCScanner.swift**
   - 添加了读取模式控制
   - 提供了便捷的切换方法：`toggleReadMode()`, `enableMinimalReadMode()`, `enableFullReadMode()`

3. **BloodGlucoseView.swift**
   - 添加了 `ReadingModeToggle` UI 组件
   - 用户可以直观地选择读取模式

## 使用建议

### 推荐做法：
1. **默认使用实时模式**：可以获得当前血糖值，避免连接丢失
2. **传感器状态检查**：确保传感器已激活且处于正常状态
3. **正确扫描姿势**：将 iPhone 贴近传感器并保持稳定

### 如果仍有连接问题：
1. 确保传感器电量充足
2. 避免在金属环境或电磁干扰强的地方扫描
3. 重启 iPhone NFC 功能
4. 尝试多次扫描（程序已内置重试机制）

## 技术细节

### DiaBLE 项目参考：
- 采用了 DiaBLE 项目的连接策略
- 使用了相同的重试机制和延迟设置
- 参考了 Libre 1 和 Libre 2+ 的不同读取方法

### 数据解析：
- **实时模式**：从最小数据中提取当前血糖值
- **完整模式**：解析完整的趋势和历史数据
- **错误处理**：即使数据不完整也尝试提取有用信息

## 预期效果

通过这些改进，您应该能够：
- ✅ 大大减少连接丢失的情况
- ✅ 快速获得当前血糖读数
- ✅ 根据需要选择读取模式
- ✅ 享受更稳定的 NFC 扫描体验

如果您只需要当前血糖值而不需要历史数据，强烈建议使用默认的"实时模式"。

